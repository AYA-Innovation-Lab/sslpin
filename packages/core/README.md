# @sslpin/core

Shared TLS inspection logic for `sslpin`.

## What it provides

- Node TLS probing via `@sslpin/core/node`
- Browser-side certificate normalization helpers
- Shared serialization, warning, and pin conversion utilities

## Build

```bash
yarn workspace @sslpin/core build
```
